/**
 * The package name for
 * {@link https://www.npmjs.com/package/@signicode/verser2-guest-bun | `@signicode/verser2-guest-bun`}.
 *
 * @public
 */
export const VERSER2_GUEST_BUN_PACKAGE_NAME = '@signicode/verser2-guest-bun';

/**
 * Error message used when a Bun route handler does not return a `Response` instance.
 *
 * @public
 */
export const DISPATCH_BUN_NOT_A_RESPONSE_MESSAGE = 'Handler must return a Response instance.';
