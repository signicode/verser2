{
  "name": "@signicode/verser2-host",
  "version": "0.0.0",
  "private": true,
  "description": "Initial TypeScript scaffold for the Verser2 Host package.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@signicode/verser-common": "0.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --target es2019 --platform node --sourcemap --clean --external @signicode/verser-common && dts-bundle-generator --project tsconfig.json --out-file dist/index.d.ts src/index.ts"
  }
}
