# Implementation Plan: GitHub Action workflow corrections

## Phase 1: Track branch, PR surface, and workflow behavior inventory

- [x] Task: Create track branch and PR review surface
    - [x] Record the current branch as the PR base before creating the track branch.
        - PR base recorded: `main`.
    - [x] Create a dedicated Conductor track branch from the current branch before implementation work.
        - Branch: `track/github-action-workflow-corrections_20260616`.
    - [x] Create a GitHub pull request using `gh` with a title/body describing the TO-BE workflow behavior: relevant PRs validate, merged source PRs publish SHA builds, nightly runs publish nightly builds, GitHub tags publish channel releases, and npmjs remains manual.
        - PR: https://github.com/signicode/verser2/pull/26
    - [x] Record the branch name, PR base, and PR URL in this plan.
- [x] Task: Inventory existing workflow, scripts, tests, and docs
    - [x] Review `.github/workflows/package-publish.yml` triggers, jobs, permissions, publish conditions, and artifact reuse.
    - [x] Review `scripts/package-version-policy.js` and release/package scripts for current version, dist-tag, main-build, and staged apply behavior.
    - [x] Review workflow/version-policy tests under `test/` and identify expected failing tests to add first.
    - [x] Review `docs/release-procedure.md`, `docs/package-publishing.md`, and `conductor/tech-stack.md` for release-behavior claims that will need updates.
    - Inventory result: package workflow used PR path filters and unfiltered main/tag push triggers, main pushes published SHA builds under `next`, tag releases used stable/prerelease policy, release docs needed updated branch/PR/nightly/npmjs manual policy language.
- [x] Task: Write failing workflow and version-policy tests first
    - [x] Add tests proving Conductor-only changes are ignored or skipped by package validation/publish automation.
    - [x] Add tests proving release-procedure/package-publishing docs trigger validation without causing package publication.
    - [x] Add tests proving merged source/package/test/workflow PRs can publish SHA builds using non-channel dist-tags.
    - [x] Add tests proving scheduled nightly publishing uses nightly versions and a nightly dist-tag without moving `latest` or `next`.
    - [x] Add tests proving `v*` stable and prerelease GitHub tags map to GitHub Packages `latest` and `next`, respectively.
    - [x] Add tests proving automated workflows do not publish to npmjs.org.
- [x] Task: Confirm tests fail for expected reasons
    - [x] Run the narrowest workflow/version-policy test command.
    - [x] Confirm failures represent missing or outdated workflow behavior rather than unrelated breakage.
    - [x] Record failure output and coverage status in this plan.
        - Initial focused command: `node --test test/package-version-policy.test.js test/package-workflow.test.js`.
        - Result before implementation: failed as expected because publish-kind support, nightly schedule, release-doc gating, Conductor-only gating, and non-channel SHA dist-tag behavior were not yet implemented.
        - Coverage: repository does not produce coverage metrics for these static workflow/script tests; meaningful coverage provided by focused Node tests and full suite validation.
- [x] Task: Commit and push Phase 1 before manual validation
    - [x] Run the narrowest lint or formatting validation needed for test/planning changes.
    - [x] Commit Phase 1 changes with a scoped message.
    - [x] Push the phase commit to the track PR branch.
    - [x] Record the commit SHA and validation results in this plan.
        - Phase work was consolidated into implementation checkpoint `938e8de` after the failing tests were confirmed and the implementation/docs were completed.
- [x] Task: Conductor - User Manual Verification 'Phase 1: Track branch, PR surface, and workflow behavior inventory' (Protocol in workflow.md)
    - User verified and accepted on 2026-06-16.

## Phase 2: Workflow trigger and publish-kind implementation

- [x] Task: Refine GitHub Actions triggers and gating
    - [x] Update pull request validation paths so source/package/test/workflow and release-engineering docs trigger validation.
    - [x] Ensure Conductor-only changes do not run package build/test/publish jobs.
    - [x] Replace ordinary main-push publication with a merged-PR-aware publish path that only runs for merged pull requests with publish-triggering source/package/test/workflow changes.
    - [x] Preserve `v*` tag release publishing.
    - [x] Add scheduled nightly publishing and any needed manual dispatch guardrails.
- [x] Task: Implement publish-kind metadata flow
    - [x] Add or update script logic that resolves publish kind: pull request validation, merged-PR SHA build, nightly build, GitHub tag release, and manual npmjs candidate.
    - [x] Compute deterministic SHA versions for merged PR builds and assign only non-channel dist-tags.
    - [x] Compute deterministic nightly versions and assign only the `nightly` dist-tag.
    - [x] Preserve stable tag release mapping to `latest` and prerelease tag release mapping to `next` for GitHub Packages.
    - [x] Keep npmjs publish eligibility manual and disabled in automated GitHub Actions.
- [x] Task: Preserve package validation and publication safety
    - [x] Ensure build, stage, staged consumer validation, tarball validation, source tests, and lint still run before any automated GitHub Packages publication.
    - [x] Ensure publish jobs use the minimum required permissions and do not publish on pull request validation runs.
    - [x] Ensure GitHub Packages install validation still runs when appropriate after publication.
- [x] Task: Validate workflow trigger and publish-kind implementation
    - [x] Run focused workflow and version-policy tests until they pass.
    - [x] Run lint for changed workflow/script/test files.
    - [x] Record validation results and any intentionally skipped full validation in this plan.
        - Focused validation: `node --test test/package-version-policy.test.js test/package-workflow.test.js` passed 31/31.
        - Lint validation: `npm run lint` passed after formatting the workflow test.
        - Full validation was also run in Phase 3.
- [x] Task: Commit and push Phase 2 before manual validation
    - [x] Commit Phase 2 changes with a scoped message.
    - [x] Push the phase commit to the track PR branch.
    - [x] Record the commit SHA and validation results in this plan.
        - Implementation checkpoint: `938e8de fix(release): correct package publish workflow`.
        - Pushed to `origin/track/github-action-workflow-corrections_20260616`.
- [x] Task: Conductor - User Manual Verification 'Phase 2: Workflow trigger and publish-kind implementation' (Protocol in workflow.md)
    - User verified and accepted on 2026-06-16.

## Phase 3: Release documentation and full validation

- [x] Task: Update release and publishing documentation
    - [x] Update `docs/release-procedure.md` with the branch/PR workflow, manual gate, Copilot review/comment fetching via `gh`, PR merge steps, merged-PR SHA publish behavior, scheduled nightly behavior, explicit GitHub tag release behavior, GitHub Packages channel advancement, and manual npmjs publication from validated GitHub Packages versions.
    - [x] Update `docs/package-publishing.md` if package-publishing behavior or registry/channel language changes.
    - [x] Update `conductor/tech-stack.md` if GitHub Actions behavior statements are no longer accurate.
- [x] Task: Final safety and deduplication review
    - [x] Review scripts for duplicated version/dist-tag logic and centralize reusable behavior in the release-engineering script layer.
        - Result: publish-kind version/dist-tag decisions are centralized in `scripts/package-version-policy.js`; workflow metadata step calls `getPolicySummary`.
    - [x] Confirm no workflow path can automatically publish to npmjs.org.
    - [x] Confirm Conductor-only changes remain validation-skipped while release-engineering docs remain validation-covered.
    - [x] Confirm public package manifests and runtime code are unchanged unless required by the workflow correction.
- [x] Task: Full validation pass
    - [x] Run `npm run lint`.
    - [x] Run `npm test` or narrower documented equivalents if full validation is not necessary.
    - [x] Run package consumer/tarball validations if package output, version policy, or publish behavior changed.
    - [x] Record skipped validation, failures, or manual-only checks with reasons.
        - `npm run lint`: passed.
        - `node --test test/package-version-policy.test.js test/package-workflow.test.js`: passed 31/31.
        - `npm test`: passed 265/269 with 4 expected skips; `npm test` includes build, staging, and repository test execution.
        - Separate networked GitHub Packages install validation is workflow/credential dependent and documented as manual/CI validation.
- [x] Task: PR handoff readiness
    - [x] Fetch Copilot PR review/comments with `gh` and record whether corrections are required.
        - `gh pr view 26 --json reviews,comments,url,state`: PR is open with no reviews and no comments at fetch time; no corrections required from PR feedback yet.
    - [x] Ensure the PR body or comments summarize workflow trigger behavior, publish kinds, dist-tags, nightly behavior, npmjs manual policy, and validation.
    - [x] Confirm all acceptance criteria from `spec.md` are addressed.
- [x] Task: Commit and push Phase 3 before manual validation
    - [x] Commit Phase 3 changes with a scoped message.
    - [x] Push the phase commit to the track PR branch.
    - [x] Record the commit SHA and validation results in this plan.
        - Implementation checkpoint: `938e8de fix(release): correct package publish workflow`.
        - Manual verification checkpoint will be committed with final track completion.
- [x] Task: Conductor - User Manual Verification 'Phase 3: Release documentation and full validation' (Protocol in workflow.md)
    - User verified and accepted on 2026-06-16.
