{
  "name": "verser2",
  "version": "0.0.0",
  "private": true,
  "description": "Reverse HTTP connectivity packages for exposing HTTP servers from client-side processes.",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace=@signicode/verser-common && npm run build --workspace=@signicode/verser2-guest-js-common && npm run build --workspace=@signicode/verser2-guest-node && npm run build --workspace=@signicode/verser2-host && npm run build --workspace=@signicode/verser2-guest-bun && npm run build --workspace=@signicode/verser2-guest-python",
    "stage:packages": "node ./scripts/stage-packages.js",
    "package:version-policy": "node ./scripts/package-version-policy.js",
    "package:prepare-release": "node ./scripts/prepare-release.js",
    "test:package-consumers": "node ./scripts/test-package-consumers.js",
    "test:package-tarballs": "node ./scripts/test-package-tarballs.js",
    "test": "npm run build && npm run stage:packages && node --test test/*.test.js",
    "test:bounded": "node ./scripts/run-bounded-tests.js",
    "test:coverage": "npm run build && npm run stage:packages && node --test --experimental-test-coverage test/*.test.js",
    "test:bounded:coverage": "node ./scripts/run-bounded-tests.js --coverage",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.14",
    "@types/node": "^20.14.0",
    "dts-bundle-generator": "^9.5.1",
    "tsup": "^8.5.0",
    "typescript": "^5.4.5",
    "undici": "^8.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
